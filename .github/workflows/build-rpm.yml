name: Build RPM

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-rpm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install RPM tools
        run: sudo apt-get update && sudo apt-get install -y rpm

      - name: Get version
        id: version
        run: echo "version=$(grep -oP '__version__ = "\K[^"]+' src/l10n_glossary/__init__.py)" >> $GITHUB_OUTPUT

      - name: Setup RPM build
        run: |
          mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          VERSION=${{ steps.version.outputs.version }}
          DIR=l10n-glossary-$VERSION
          mkdir -p $DIR/{l10n_glossary,data/icons,data/man,po/sv/LC_MESSAGES}
          cp src/l10n_glossary/*.py $DIR/l10n_glossary/
          cp data/se.danielnylander.l10n-glossary.desktop $DIR/data/
          cp data/icons/se.danielnylander.l10n-glossary.svg $DIR/data/icons/
          cp data/man/l10n-glossary.1 $DIR/data/man/
          cp LICENSE $DIR/
          [ -f po/sv/LC_MESSAGES/l10n-glossary.mo ] && cp po/sv/LC_MESSAGES/l10n-glossary.mo $DIR/po/sv/LC_MESSAGES/
          tar czf ~/rpmbuild/SOURCES/$DIR.tar.gz $DIR

      - name: Create spec file
        run: |
          cat > ~/rpmbuild/SPECS/l10n-glossary.spec << 'SPEC'
          Name:           l10n-glossary
          Version:        VERSION_PLACEHOLDER
          Release:        1
          Summary:        Glossary Editor for localization projects
          License:        GPL-3.0-or-later
          URL:            https://github.com/yeager/l10n-glossary
          Source0:        %{name}-%{version}.tar.gz
          BuildArch:      noarch
          Requires:       python3 >= 3.9
          Requires:       python3-gobject
          Requires:       gtk4
          Requires:       libadwaita

          %description
          A GTK4/Adwaita glossary editor for managing translation glossaries
          and ensuring consistency across localization projects.

          %prep
          %setup -q

          %install
          mkdir -p %{buildroot}%{_datadir}/l10n-glossary/l10n_glossary
          cp l10n_glossary/*.py %{buildroot}%{_datadir}/l10n-glossary/l10n_glossary/

          mkdir -p %{buildroot}%{_bindir}
          cat > %{buildroot}%{_bindir}/l10n-glossary << 'LAUNCHER'
          #!/bin/bash
          exec python3 -c "import sys; sys.path.insert(0, '/usr/share/l10n-glossary'); from l10n_glossary.main import main; main()" "$@"
          LAUNCHER
          chmod 755 %{buildroot}%{_bindir}/l10n-glossary

          mkdir -p %{buildroot}%{_datadir}/applications
          cp data/se.danielnylander.l10n-glossary.desktop %{buildroot}%{_datadir}/applications/

          mkdir -p %{buildroot}%{_datadir}/icons/hicolor/scalable/apps
          cp data/icons/se.danielnylander.l10n-glossary.svg %{buildroot}%{_datadir}/icons/hicolor/scalable/apps/

          mkdir -p %{buildroot}%{_mandir}/man1
          cp data/man/l10n-glossary.1 %{buildroot}%{_mandir}/man1/

          mkdir -p %{buildroot}%{_datadir}/locale/sv/LC_MESSAGES
          [ -f po/sv/LC_MESSAGES/l10n-glossary.mo ] && cp po/sv/LC_MESSAGES/l10n-glossary.mo %{buildroot}%{_datadir}/locale/sv/LC_MESSAGES/

          %files
          %license LICENSE
          %{_bindir}/l10n-glossary
          %{_datadir}/l10n-glossary/l10n_glossary/__init__.py
          %{_datadir}/l10n-glossary/l10n_glossary/consistency.py
          %{_datadir}/l10n-glossary/l10n_glossary/glossary.py
          %{_datadir}/l10n-glossary/l10n_glossary/io_handler.py
          %{_datadir}/l10n-glossary/l10n_glossary/main.py
          %{_datadir}/applications/se.danielnylander.l10n-glossary.desktop
          %{_datadir}/icons/hicolor/scalable/apps/se.danielnylander.l10n-glossary.svg
          %{_mandir}/man1/l10n-glossary.1*
          %{_datadir}/locale/sv/LC_MESSAGES/l10n-glossary.mo
          SPEC
          sed -i "s/VERSION_PLACEHOLDER/${{ steps.version.outputs.version }}/" ~/rpmbuild/SPECS/l10n-glossary.spec

      - name: Build RPM
        run: rpmbuild -ba ~/rpmbuild/SPECS/l10n-glossary.spec

      - name: Upload RPM to release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${{ steps.version.outputs.version }}
          gh release upload "v$VERSION" \
            ~/rpmbuild/RPMS/noarch/l10n-glossary-${VERSION}-1.noarch.rpm \
            --clobber
